{
  "name": "react-native-letote-fast-image",
  "version": "1.3.1",
  "description": "🚩 FastImage, performant React Native image component.",
  "keywords": [
    "cache",
    "cached",
    "fastimage",
    "image",
    "priority"
  ],
  "license": "MIT",
  "author": "blakefang",
  "files": [
    "ios",
    "android",
    "utils",
    "FastImage.js",
    "index.d.ts"
  ],
  "main": "FastImage.js",
  "directories": {
    "example": "example"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fangasvsass/react-native-fast-image.git"
  },
  "scripts": {
    "format": "prettier --write --no-semi --single-quote --trailing-comma all ./FastImage.js ./example/*.js ./example/fastImage/*.js ./server/*.js",
    "prepare": "git submodule update --init --recursive",
    "test": "yarn run test:pretty && yarn run test:jest",
    "test:jest": "jest *.js",
    "test:pretty": "prettier-check --write --no-semi --single-quote --trailing-comma all ./FastImage.js ./example/*.js ./example/fastImage/*.js ./server/*.js"
  },
  "dependencies": {
    "prop-types": "^15.6.0"
  },
  "devDependencies": {
    "babel-jest": "^21.2.0",
    "babel-preset-react-native": "^4.0.0",
    "jest": "^21.2.1",
    "prettier": "^1.5.2",
    "prettier-check": "^2.0.0",
    "react": "16.0.0",
    "react-native": "^0.56.0",
    "react-test-renderer": "16.0.0"
  },
  "jest": {
    "preset": "react-native",
    "modulePathIgnorePatterns": [
      "example",
      "server"
    ]
  }
}
